---
name:  tui na ba fa clinical application.Value.ToUpper() ui  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() linical  tui na ba fa clinical application.Value.ToUpper() pplication
description: 本技能提供推拿八法（即按、摩、推、拿、揉、捏、点、拍）的核心操作规范与临床应用准则。当用户需要根据中医辨证结果选择合适的推拿手法、确定操作顺序、施力强度或适应症/禁忌症判断时，应使用此技能。
tags: [application, clinical, manipulation, tcm, tui, tuina]
---

#  tui na ba fa clinical application.Value.ToUpper() ui  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() a  tui na ba fa clinical application.Value.ToUpper() linical  tui na ba fa clinical application.Value.ToUpper() pplication

## When to Use
- 用户咨询推拿治疗的具体手法选择
- 需要根据病症（如风寒湿痹、气滞血瘀、脏腑失调等）匹配对应推拿方法
- 判断某症状是否适合采用推拿干预
- 确定推拿操作的先后顺序与组合逻辑

## Execution Steps
1. **辨证分型**：明确患者证候类型（如表里、寒热、虚实）。
2. **选择主法**：依据证型选取主导手法（例如：实证多用推、拿；虚证多用摩、揉）。
3. **配伍辅法**：结合主法搭配辅助手法以增强疗效或缓解不适。
4. **确定参数**：设定操作频率、力度、持续时间及部位。
5. **验证禁忌**：排除推拿禁忌症（如皮肤破损、急性炎症、严重骨质疏松等）。
6. **动态调整**：根据患者反应实时调整手法强度与节奏。

> 注意：推拿八法不可孤立使用，必须基于整体辨证和个体差异进行组合应用。